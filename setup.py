from setuptools import setup, find_packages

setup(
    name='travel_route_optimization',
    version='0.1',
    packages=find_packages(),
    # install_requires=[
    #     # Add any external dependencies here
    # ],
)